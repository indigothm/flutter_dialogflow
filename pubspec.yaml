name: flutter_dialogflow_v2
description: This package is useful to integrate dialogflow v2 in your Flutter project, it provides detectIntent request and response data with json mapping.
version: 0.3.3
homepage: https://diegodalbosco.github.io/flutter_dialogflow/

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  googleapis_auth: ^1.1.0
  meta: ^1.1.6

dev_dependencies:
  test:
  flutter_test:
    sdk: flutter

environment:
  sdk: '>=2.1.0 <3.0.0'
